services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aniwhere-ai
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - chromadb-data:/app/chromadb
    restart: unless-stopped

volumes:
  chromadb-data:
